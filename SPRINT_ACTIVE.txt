# ============================================================================
# v15 = SUPERVISED BLEND of lgb+FE & realmlp+FE (2026-06-03). v14 BROKE the ceiling: realmlp+FE 0.9690,
# lgb+FE 0.9682, ρ=0.85 (decorrelated AND both strong) = textbook blend setup. Re-runs the deterministic
# v14 race (reproduces both OOF+test — v14 only saved test), then fits 3 blends on the labeled dev OOF
# (Caruana / LogReg-on-logits [Deotte's move] / mean), selects on the held-out holdout, writes v15_final.
# Persists OOF (probs/v15_*_oof.npy) so future blends skip the re-run. Verified locally (all 3 blends run).
# GPU REQUIRED (~25 min, re-runs the RealMLP race). Target: clear 0.9700.
#   v14 race: notebooks/14_v14_realmlp_fe.py | v13 bare RealMLP: notebooks/13_v13_realmlp.py.
#   NOTE: v14 results SUPERSEDE the old locked finals (0.96641) — re-pick finals around v14/v15.
# ============================================================================
notebooks/15_v15_blend.py
