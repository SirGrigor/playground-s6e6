# ============================================================================
# v16 = 3-LEG BLEND: lgb+FE + realmlp+FE + CatBoost+FE (2026-06-03). v15 LogReg-stacker of 2 legs →
# holdout 0.9694 / LB 0.96933 (passed public Demidov 0.96920). v16 adds CatBoost+FE (native cats =
# ordered-TS strength) as a 3rd algo-diverse leg, re-blends 3-way (Caruana/LogReg-logits/mean), selects
# on holdout. Honest prior: CatBoost is another GBDT (v6 ρ0.9 w/ LGBM) → marginal new diversity, but the
# stacker zero-weights it if redundant = LOW RISK. Verified locally (all 3 legs train, blends run).
# GPU REQUIRED (~30-35 min: RealMLP race + CatBoost ×5). Target: clear 0.9700 (−0.0006 to go).
#   v15 2-way blend: notebooks/15_v15_blend.py. FINALS so far: anchor v15_final 0.96933 + hedge v14_realmlp 0.96893.
#   If CatBoost underwhelms → higher-diversity levers: seed-bag RealMLP + RealMLP Optuna sweep.
# ============================================================================
notebooks/16_v16_catboost_blend.py
